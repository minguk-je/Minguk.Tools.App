namespace Minguk.Tools.Views;

/// <summary>Automation 화면. 위에서 솔루션·프로젝트를 고르고 아래 탭이 그것을 따라간다(TamsTools StreamMode 와 같은 짜임).</summary>
public partial class AutomationMainView : System.Windows.Controls.UserControl
{
    public AutomationMainView()
    {
        InitializeComponent();
    }
}

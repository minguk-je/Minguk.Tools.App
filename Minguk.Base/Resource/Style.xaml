<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                    xmlns:dxa="http://schemas.devexpress.com/winfx/2008/xaml/accordion"
                    xmlns:dxat="http://schemas.devexpress.com/winfx/2008/xaml/accordion/themekeys"
                    xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
                    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
                    xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
                    xmlns:dxgt="http://schemas.devexpress.com/winfx/2008/xaml/grid/themekeys"
                    xmlns:dxi="http://schemas.devexpress.com/winfx/2008/xaml/core/internal"
                    xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
                    xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys"
                    xmlns:av="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:controls="clr-namespace:Minguk.Base.Controls"
                    xmlns:sys="clr-namespace:System;assembly=System.Runtime">

    <!--
        BaseFontFamily / BaseFontSize 는 TamsTools/App.xaml 에 정의되어 있다.
        이 사전은 Minguk.Base 어셈블리 쪽이라 파싱 시점에 애플리케이션 리소스를 볼 수 없으므로
        StaticResource 로 쓰면 해석에 실패한 StaticResourceExtension 이 Setter.Value 에 그대로 남고,
        Style.Seal() 에서 "Setter.Value 에 StaticResourceExtension 을 쓸 수 없습니다" 예외가 난다.
        조회를 런타임으로 미루는 DynamicResource 를 써야 한다. (아래 CheckEdit / DateEdit 과 동일)
    -->
    <Style TargetType="dxe:TextEdit">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>

    <Style TargetType="dxe:ComboBoxEdit">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>

    <Style TargetType="dxe:SpinEdit">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>

    <Style TargetType="{x:Type dxe:CheckEdit}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>

    <Style TargetType="{x:Type dxe:DateEdit}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>

    <!--
        아래 타입들도 위와 같은 이유로 여기서 폰트를 잡는다.
        
        WPF 암시적 스타일은 TargetType 이 정확히 일치할 때만 붙는다 — 파생 타입에는 상속되지 않는다.
        그래서 dxe:BaseEdit 스타일이 있어도 ButtonEdit / PasswordBoxEdit 에는 안 붙고,
        dxg:GridControl 스타일이 있어도 controls:BaseGridControl 에는 안 붙는다.
        타입마다 하나씩 적어 주는 것 외에 방법이 없다.
        
        이걸 여기 모아 두는 이유는, 예전처럼 각 View 가 FontFamily/FontSize 를 직접 적으면
        타이포그래피 정책이 40여 개 파일에 흩어져서 한 번 바꾸려면 40곳을 고쳐야 하기 때문이다.
    -->
    <Style TargetType="{x:Type dxe:ButtonEdit}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>

    <Style TargetType="{x:Type dxe:PasswordBoxEdit}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>

    <Style TargetType="{x:Type dxb:ToolBarControl}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>

    <Style TargetType="{x:Type dxa:AccordionControl}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>

    <Style TargetType="{x:Type controls:BaseGridControl}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>

    <Style TargetType="{x:Type controls:BaseTableView}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>

    <Style TargetType="{x:Type TreeView}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>

    <Style TargetType="{x:Type FlowDocumentScrollViewer}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>

    <Style TargetType="{x:Type dx:SimpleButton}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>

    <Style TargetType="{x:Type dxlc:LayoutItem}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>

    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="BaseTextBlock"
           TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="InfoTextBlock"
           TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="H1TextBlock"
           TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="24pt" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="H2TextBlock"
           TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="18pt" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="H3TextBlock"
           TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="13.55pt" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!-- 기본 -->
    <Style x:Key="H4TextBlock"
           TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="12pt" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="H5TextBlock"
           TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="10pt" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="H6TextBlock"
           TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="7.55pt" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>


    <Style TargetType="{x:Type FrameworkElement}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="av:TextOptions.TextFormattingMode" Value="Display" />
        <Setter Property="av:TextOptions.TextRenderingMode" Value="ClearType" />
        <Setter Property="UseLayoutRounding" Value="True" />
    </Style>

    <Style x:Key="BaseFrameworkElement"
           TargetType="{x:Type FrameworkElement}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="av:TextOptions.TextFormattingMode" Value="Display" />
        <Setter Property="av:TextOptions.TextRenderingMode" Value="ClearType" />
        <Setter Property="UseLayoutRounding" Value="True" />
    </Style>

    <Style x:Key="BaseControlStyle"
           BasedOn="{StaticResource BaseFrameworkElement}"
           TargetType="{x:Type Control}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>


    <Style TargetType="{x:Type dxe:BaseEdit}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>

    <Style x:Key="GridControl.FontStyle"
           TargetType="{x:Type dxg:GridControl}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>


    <Style TargetType="{x:Type Control}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>

    <Style TargetType="{x:Type dxe:SearchControl}">
        <Setter Property="FontFamily" Value="{DynamicResource ResourceKey=BaseFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ResourceKey=BaseFontSize}" />
    </Style>

    <Style TargetType="{x:Type dxb:BarStaticItem}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock VerticalAlignment="Center"
                               Text="{Binding}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type dxb:BarButtonItem}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock VerticalAlignment="Center"
                               Text="{Binding}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type dxb:BarCheckItem}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock VerticalAlignment="Center"
                               Text="{Binding}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type dxb:BarEditItem}">
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock VerticalAlignment="Center"
                               Text="{Binding}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="Content2Template">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock VerticalAlignment="Center"
                               Text="{Binding}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <!--
    <Style TargetType="dxlc:Tile">
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Opacity="0.5"
                                  BlurRadius="4"
                                  Direction="330"
                                  ShadowDepth="2"
                                  Color="Black" />
            </Setter.Value>
        </Setter>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="dxlc:Tile">
                    <Border Name="root"
                            Padding="0"
                            Background="{TemplateBinding Background}"
                            dx:FrameworkElementHelper.IsClipped="True"
                            CornerRadius="10"
                            RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform>
                            <ScaleTransform />
                        </Border.RenderTransform>
                        <Border.Resources>
                            <dx:MultiplyConverter x:Key="MultiplyConverter" />
                            <Storyboard x:Key="ContentChangeStoryboard" x:Name="ContentChangeStoryboard"
                                        FillBehavior="Stop">
                                <ObjectAnimationUsingKeyFrames Duration="0:0:0.15"
                                                               FillBehavior="Stop"
                                                               Storyboard.TargetName="previousContent"
                                                               Storyboard.TargetProperty="Visibility">
                                    <ObjectAnimationUsingKeyFrames.KeyFrames>
                                        <DiscreteObjectKeyFrame KeyTime="0">
                                            <DiscreteObjectKeyFrame.Value>
                                                <Visibility>Visible</Visibility>
                                            </DiscreteObjectKeyFrame.Value>
                                        </DiscreteObjectKeyFrame>
                                    </ObjectAnimationUsingKeyFrames.KeyFrames>
                                </ObjectAnimationUsingKeyFrames>
                                <DoubleAnimation Duration="0:0:0.15"
                                                 Storyboard.TargetName="previousContent"
                                                 Storyboard.TargetProperty="(RenderTransform).Y"
                                                 To="{Binding ElementName=root, Path=ActualHeight, Converter={StaticResource MultiplyConverter}, ConverterParameter=-1}" />
                                <DoubleAnimationUsingKeyFrames Duration="0:0:1"
                                                               Storyboard.TargetName="content"
                                                               Storyboard.TargetProperty="(RenderTransform).Y">
                                    <DiscreteDoubleKeyFrame KeyTime="0"
                                                            Value="{Binding ElementName=root, Path=ActualHeight}" />
                                    <DiscreteDoubleKeyFrame KeyTime="0:0:0.15"
                                                            Value="{Binding ElementName=root, Path=ActualHeight}" />
                                    <LinearDoubleKeyFrame KeyTime="0:0:0.4"
                                                          Value="20" />
                                    <EasingDoubleKeyFrame KeyTime="0:0:1"
                                                          Value="0">
                                        <EasingDoubleKeyFrame.EasingFunction>
                                            <CubicEase EasingMode="EaseOut" />
                                        </EasingDoubleKeyFrame.EasingFunction>
                                    </EasingDoubleKeyFrame>
                                </DoubleAnimationUsingKeyFrames>
                            </Storyboard>
                        </Border.Resources>
                        <Grid>
                            ~1~<Rectangle Fill="{TemplateBinding CalculatedBackground}" />@1@

                            <ContentPresenter Name="content"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding CurrentContentTemplate}">
                                <ContentPresenter.RenderTransform>
                                    <TranslateTransform />
                                </ContentPresenter.RenderTransform>
                            </ContentPresenter>
                            <ContentPresenter Name="previousContent"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Content="{TemplateBinding PreviousContent}"
                                              ContentTemplate="{TemplateBinding CurrentContentTemplate}"
                                              Visibility="Collapsed">
                                <ContentPresenter.RenderTransform>
                                    <TranslateTransform />
                                </ContentPresenter.RenderTransform>
                            </ContentPresenter>
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalHeaderAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalHeaderAlignment}"
                                              Content="{TemplateBinding Header}"
                                              ContentTemplate="{TemplateBinding HeaderTemplate}"
                                              Visibility="{Binding Path=(dxlc:Tile.CalculatedHeaderVisibility), RelativeSource={RelativeSource TemplatedParent}}" />
                            ~1~<Border BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" />@1@
                        </Grid>

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0:0:0.1"
                                                      To="Pressed" />
                                    <VisualTransition From="Pressed"
                                                      GeneratedDuration="0:0:0.1" />
                                    <VisualTransition GeneratedDuration="0:0:0.1"
                                                      To="MouseOver" />
                                    <VisualTransition From="MouseOver"
                                                      GeneratedDuration="0:0:0.1" />
                                </VisualStateGroup.Transitions>

                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <DoubleAnimation Duration="0"
                                                         Storyboard.TargetName="root"
                                                         Storyboard.TargetProperty="(RenderTransform).ScaleX"
                                                         To="1.01" />
                                        <DoubleAnimation Duration="0"
                                                         Storyboard.TargetName="root"
                                                         Storyboard.TargetProperty="(RenderTransform).ScaleY"
                                                         To="1.01" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <DoubleAnimation Duration="0"
                                                         Storyboard.TargetName="root"
                                                         Storyboard.TargetProperty="(RenderTransform).ScaleX"
                                                         To="0.99" />
                                        <DoubleAnimation Duration="0"
                                                         Storyboard.TargetName="root"
                                                         Storyboard.TargetProperty="(RenderTransform).ScaleY"
                                                         To="0.99" />
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    -->


    <Style x:Key="AnimationTextBlock"
           TargetType="TextBlock">
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Opacity="0.5"
                                  BlurRadius="4"
                                  Direction="330"
                                  ShadowDepth="2"
                                  Color="Black" />
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <EventTrigger.Actions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Duration="0:0:0.100"
                                             Storyboard.TargetProperty="FontSize"
                                             To="50" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger.Actions>
            </EventTrigger>

            <EventTrigger RoutedEvent="MouseLeave">
                <EventTrigger.Actions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Duration="0:0:0.100"
                                             Storyboard.TargetProperty="FontSize"
                                             To="40" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger.Actions>
            </EventTrigger>
        </Style.Triggers>
    </Style>


    <Style x:Key="BaseSimpleButton"
           TargetType="{x:Type dx:SimpleButton}">
        <Style.Triggers>
            <Trigger Property="IsChecked" Value="False">
                <Setter Property="Background" Value="{dxi:ThemeResource {dxt:PaletteBrushThemeKey ResourceKey=WindowBackground}}" />
            </Trigger>
        </Style.Triggers>
    </Style>


    <x:Array x:Key="BackgroundColors"
             Type="{x:Type Color}">
        <!-- <Color>#c1e9fe</Color> -->
        <!-- <Color>#b3e5b8</Color> -->
        <!-- <Color>#fdc99d</Color> -->
        <!-- <Color>#d7d7d7</Color> -->
        <!-- <Color>#f8ea92</Color> -->
        <!-- <Color>#9febe3</Color> -->
        <!-- <Color>#f6b7da</Color> -->
        <!-- <Color>#dbbba4</Color> -->
        <!-- <Color>#f9a7b3</Color> -->
        <Color>#FFFFC2BE</Color>
        <Color>#FFA8D5FF</Color>
        <Color>#FFC1F49C</Color>
        <Color>#FFF3E4C7</Color>
        <Color>#FFF4CE93</Color>
        <Color>#FFC7F4FF</Color>
        <Color>#FFCFDB98</Color>
        <Color>#FFE0CFE9</Color>
        <Color>#FF8DE9DF</Color>
        <Color>#FFFFF7A5</Color>
    </x:Array>

    <Style x:Key="DottedHorizontalGridSplitter"
           TargetType="GridSplitter">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Height" Value="6" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GridSplitter">
                    <Grid Background="{TemplateBinding Background}">
                        <dxlc:ElementSizer HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False"
                                           Side="TopBottom" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DottedVerticalGridSplitter"
           TargetType="GridSplitter">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Width" Value="6" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GridSplitter">
                    <Grid Background="{TemplateBinding Background}">
                        <dxlc:ElementSizer HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False"
                                           Side="LeftRight" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
                    xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
                    xmlns:dxgt="http://schemas.devexpress.com/winfx/2008/xaml/grid/themekeys"
                    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
                    xmlns:helpers="http://schemas.devexpress.com/winfx/2008/xaml/editors/internal"
                    xmlns:themes="http://schemas.devexpress.com/winfx/2008/xaml/editors/themekeys">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Style.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <dxmvvm:NumericToVisibilityConverter x:Key="NumericToVisibilityConverter"
                                         Inverse="True" />

    <ControlTemplate x:Key="{themes:TextEditThemeKey ResourceKey=EditTemplate, IsThemeIndependent=True}">
        <TextBox x:Name="PART_Editor"
                 Style="{DynamicResource {themes:TextEditThemeKey ResourceKey=TextStyle}}"
                 helpers:EditorMarginHelper.Margin="2,2,2,2,0,2,2,2"
                 InputMethod.PreferredImeConversionMode="{Binding Path=(dxe:BaseEdit.OwnerEdit).(InputMethod.PreferredImeConversionMode)}"
                 InputMethod.PreferredImeState="{Binding Path=(dxe:BaseEdit.OwnerEdit).(InputMethod.PreferredImeState)}" />
    </ControlTemplate>

    <ControlTemplate x:Key="{dxgt:TableViewThemeKey ResourceKey=DataPresenterTemplate, IsThemeIndependent=True}"
                     TargetType="{x:Type dxg:DataPresenter}">
        <Border Background="Transparent">
            <Grid>
                <ContentPresenter />
                <TextBlock HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Opacity="0.8"
                           Text="표시할 데이터가 없습니다."
                           Visibility="{Binding VisibleRowCount, Converter={StaticResource NumericToVisibilityConverter}, RelativeSource={RelativeSource AncestorType=dxg:GridControl}}" />
            </Grid>
        </Border>
    </ControlTemplate>

</ResourceDictionary>
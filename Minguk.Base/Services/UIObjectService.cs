namespace Minguk.Base.Services;

/*
public class UIObjectService : ServiceBase, IUIObjectService
{
    /*
    public static readonly DependencyProperty ObjectProperty =
        DependencyProperty.Register("Object", typeof(object), typeof(ObjectService), new PropertyMetadata());

    public object Object
    {
        get { return GetValue(ObjectProperty); }
        set { SetValue(ObjectProperty, value); }
    }
    #1#
    public object Object => AssociatedObject;

    protected override void OnAttached()
    {
        if (AssociatedObject == null)
            throw new InvalidOperationException("This service can be attached only to the object.");

        base.OnAttached();
    }
}
*/

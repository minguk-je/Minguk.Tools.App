<dx:SplashScreenWindow x:Class="Minguk.Base.SplashScreen.ProgressSplashScreenWindow"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                       xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
                       xmlns:dxt="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys"
                       Style="{DynamicResource BaseControlStyle}"
                       AllowTheming="True"
                       SizeToContent="WidthAndHeight"
                       Topmost="True"
                       UseLayoutRounding="True"
                       WindowStartupLocation="CenterScreen"
                       mc:Ignorable="d"
                       d:DataContext="{x:Static dxmvvm:DXSplashScreenViewModel.DesignTimeData}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Minguk.Base;component/Resource/Style.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <dx:BackgroundPanel>
        <Grid>
            <Border CornerRadius="3">
                <Grid Margin="20,20,20,20">
                    <StackPanel HorizontalAlignment="Center"
                                VerticalAlignment="Bottom">

                        <TextBlock Margin="10,0,10,6"
                                   HorizontalAlignment="Center"
                                   Text="{Binding Status}" />

                        <ProgressBar Style="{DynamicResource ResourceKey={dxt:SplashScreenThemeKey ResourceKey=ProgressBarStyle}}"
                                     IsIndeterminate="{Binding IsIndeterminate}"
                                     Value="{Binding Progress}" />

                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </dx:BackgroundPanel>
</dx:SplashScreenWindow>
